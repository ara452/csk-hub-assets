# csk-hub-assets

Public static assets for the CSK Hub (the Hub code itself lives in a separate private repo).

- `favicon.png` — 128×128 CSK Hub favicon: "CSK" over "HUB" (Fredoka One) in deep/mid sage on a light sage tile. Referenced by the Hub's `setFaviconUrl()`, which requires a public image URL.
