# csk-hub-assets

Public static assets for the CSK Hub (the Hub code itself lives in a separate private repo).

- `favicon.png` — 64×64 CSK Hub favicon (three pillars: Clarity / Speed / Kindness, sage→forest gradient). Referenced by the Hub's `setFaviconUrl()`, which requires a public image URL.
